#include "types.h"
#include "stat.h"
#include "user.h"

int
main(void)
{
	// toggle(); // This toggles the system trace on or off
	sleep(100);	
	printf(1, "\nsum of 2 and 3 is: %d\n",add(2,3));
	sleep(100);
	exit();
}
